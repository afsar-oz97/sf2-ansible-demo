- name: set fact
  hosts:  all
  vars_files: 
    - configuration.yml

  tasks:
    - name: Sequence 1
      debug:
        msg: "{{item}}"
      register: result
      failed_when: item == 3
      with_sequence: start=0 end=5
